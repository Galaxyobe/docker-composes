version: "3.1"

services:
  db:
    image: postgres
    container_name: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres

  adminer:
    image: adminer
    container_name: adminer
    restart: always
    ports:
      - 8080:8080
